upstream {{ app_name }} {
    server {{ socket_file }};
}

server {
    listen {{ nginx_listen_port }} default_server;

    location / {
        include proxy_params;
        proxy_pass http://{{ app_name }};
    }
}
